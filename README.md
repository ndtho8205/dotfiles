# dotfiles

## Requirements

Set `zsh` as your login shell:

```shell
chsh -s $(which zsh)
```

## Install

```shell
git clone --recursive https://github.com/thoughtbot/dotfiles.git
cd dotfiles
./install.sh
```
